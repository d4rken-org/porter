plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android"
}

android {
    namespace = "eu.darken.porter.updatefixture"
    defaultConfig {
        applicationId = "eu.darken.porter.updatefixture"
        versionCode = 1
        versionName = "1"
    }
}
androidComponents.beforeVariants(androidComponents.selector().withBuildType("release")) { it.enable = false }

dependencies {
    implementation project(":server")
    implementation project(":server-shared")
    implementation project(":shared")
    implementation project(":aidl")
    implementation project(":common")
    implementation project(":porsh")
    implementation project(":shizuku-compat")
    compileOnly libs.hidden.stub
}
